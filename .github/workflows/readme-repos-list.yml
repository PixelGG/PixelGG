name: README Repos List

permissions:
  contents: write

on:
  schedule:
    - cron: "0 0 * * 1"
  workflow_dispatch:

jobs:
  update-projects:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Update repo list in README
        uses: koj-co/readme-repos-list@master
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          # Nur Repos mit Topic "profile-project" anzeigen
          query: "user:PixelGG fork:false topic:profile-project"
          max: 6
          path: "README.md"
          start: "readme-repos-list"
          end: "readme-repos-list"
          sort: "updated"
          order: "desc"
          prefix: "<p align=\"center\">"
          suffix: "</p>"
